.hero-unit  
  %h1#logo #{@app.name}
  %h2  
    = @app.keywords.sample
  %p 
    = @app.collabfinder_what
    %a{:class=>"btn btn-primary btn-large" :href=>"/a/map?seed=#{@app.seed}"} Check out the Data!
%div{:data=>{:role=>"content"}}
  .span4
    %a{:data=>{:role=>"button", :icon=>"star", :theme=>"#{@app.jquerytheme}"}, :href=> "/a/map?seed=#{@app.seed}"} #{Thesaurus::VIEW.sample.capitalize} Map
  .span4  
    %a{:data=>{:role=>"button", :theme=>"#{@app.jquerytheme}"}, :href=>"/a/contact?seed=#{@app.seed}"} #{Thesaurus::CONTACT.sample.capitalize}
  .span4
    %a{:data=>{:role=>"button", :theme=>"#{@app.jquerytheme}"}, :href=>"/a/info?seed=#{@app.seed}"} #{Thesaurus::ABOUT.sample.capitalize}
